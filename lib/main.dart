import 'package:assignment_no3/HomePage.dart';
import 'package:flutter/material.dart';

void main() => runApp(Slidable());

class Slidable extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: HomePage(),
    );
  }
}
